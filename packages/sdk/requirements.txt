# ── Core dependencies ────────────────────────────────────────────────
google-adk>=1.18.0
a2a-sdk>=0.2.0

# ── BNBAgent SDK (keystore + ERC-8004 identity) ─────────────────────
bnbagent>=0.2.0

# ── Phase 2: BSC on-chain integration ───────────────────────────────
web3>=6.15.0
python-dotenv>=1.0.0

# ── Phase 2: Greenfield decentralized storage ──────────────────────
# Uses SP REST API directly — no greenfield-python-sdk or Go library needed.
# Only requires web3 (above) + requests for HTTP.
requests>=2.31.0
