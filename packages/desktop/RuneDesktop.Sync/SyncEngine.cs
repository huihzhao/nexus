// [DELETED — Phase B] RuneDesktop.Sync was an experimental
// secondary-sync project that became dead code after Round 2 made the
// desktop a thin client. This file is kept as an empty placeholder
// because the .sln already excludes the project (task #31). Run
// ``git rm -rf packages/desktop/RuneDesktop.Sync/`` to remove for real.
namespace RuneDesktop.Sync;

// Empty — SyncEngine type removed.
