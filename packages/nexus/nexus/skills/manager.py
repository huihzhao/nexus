"""Removed in Phase E — duplicate of :mod:`nexus_core.skills.manager`.

The two files were identical apart from a stale docstring; Phase E
keeps the SDK copy as the single source of truth.

Tombstone because the workspace shell can't delete files.
"""

raise ImportError(
    "nexus.skills.manager was removed in Phase E. "
    "Use ``nexus_core.skills.manager`` (or ``nexus_core.skills``) instead."
)
