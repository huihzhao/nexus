"""Removed in Phase E — duplicate of :mod:`nexus_core.tools.web_search`.

The two files were byte-identical; Phase E keeps the SDK copy as
the single source of truth.

Tombstone because the workspace shell can't delete files.
"""

raise ImportError(
    "nexus.tools.web_search was removed in Phase E. "
    "Use ``nexus_core.tools.web_search`` (or ``nexus_core.tools``) instead."
)
