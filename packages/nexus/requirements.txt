# Rune Protocol SDK (core dependency — includes LLM Client)
rune-sdk @ file:../sdk

# LLM providers (install the one you use)
google-genai>=1.0.0        # Gemini (default)
# openai>=1.0.0            # OpenAI
# anthropic>=0.30.0        # Anthropic Claude

# File processing (for web demo uploads)
pypdf>=4.0.0               # PDF text extraction
python-docx>=1.0.0         # DOCX text extraction
openpyxl>=3.1.0            # Excel text extraction
