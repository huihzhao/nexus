"""Removed — never had a caller (Phase C placeholder).

Use ``from nexus_server.chat import files`` (re-exported in the
package __init__.py) or import the canonical module directly:
``from nexus_server.files import …``.
"""

raise ImportError(
    "nexus_server.chat.files was a Phase C placeholder with no "
    "callers — removed during dead-code cleanup. "
    "Use ``from nexus_server.files import …`` instead."
)
